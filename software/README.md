# Software

This directory is reserved for the smallest necessary hardware adaptation layer.
The preferred design keeps the host-side Dynamixel and policy contracts intact;
camera, depth, and audio drivers may need DuckLite-specific configuration.
